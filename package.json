{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna run bootstrap",
    "watch": "lerna run watch --parallel",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.3.0"
  }
}
